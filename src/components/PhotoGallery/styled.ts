import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`;

export const Image = styled.img`
    max-width: 200px;
    max-height: 200px;
    object-fit: cover;
`;